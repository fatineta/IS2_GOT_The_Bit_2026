
  # Got  the bit

  This is a code bundle for Got  the bit. The original project is available at https://www.figma.com/design/ALNQiFDJiB996uL7ebkLN6/Got--the-bit.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  